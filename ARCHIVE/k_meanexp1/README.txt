In this folder, the K_means would be applied to a preprocessed generated dataset, with three variables, three types of failures.
Any variance of experiment shall be documented and descripted in this file.

characteristics:
In the "fault_detection.json" the "is_fault" is zero when it isn't a fault and  one when it's a fault, any type of fault.




For the first example, we experiment k_means with 3 clusters.
The second experiment is defined to appling the elbow method to see.
    It was said to not be a appropiate when applied to a high dimensional dataset, therefore it was applied the gap statistic, the davis -bouldin index and the silhoute score. 
    In the elbow method, the k = 4. 
    In the gap statistic, the k = 3.
    In the davis-bouldin index, the k = 
    In the silhoute score, the k = 
The third experiment is defined to experiment if it can be defined any evaluation, therefore the idea is to divided by the dataset available.
